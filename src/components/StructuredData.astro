---
const { data } = Astro.props as { data: object | object[] };
const items = Array.isArray(data) ? data : [data];
const hasItems = items.length > 0;
---

{hasItems && <script is:inline type="application/ld+json" set:html={JSON.stringify(items.length === 1 ? items[0] : items)} />}
